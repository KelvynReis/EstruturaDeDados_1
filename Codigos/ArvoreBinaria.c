#include <stdio.h>
#include <stdlib.h>

// arvore binaria

typedef struct binaria{
	int info;
	struct nodetype *left;
	struct nodetype *right;
} bin; // tipo da struct
bin *arvore

// arvore convencional

typedef struct ConB{
	int info;
	conv *left;
	conv *right;
}conv;
conv *aux
